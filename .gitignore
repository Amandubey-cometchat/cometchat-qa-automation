# Repo-wide safety net. Each <feature>-automation/ project keeps its own
# .gitignore for its own build output — these patterns just make sure no
# project, current or future, can ever commit secrets or dependencies.
.env
.env.*
!.env.example
node_modules/
.DS_Store
*.log
