node_modules/
.env
.env.*
!.env.example

# Downloaded by `npm run setup:sample-app` — pinned upstream code, not ours
sample-app/

# Playwright output
test-results/
reports/html/
reports/junit/

.DS_Store
*.log

# Toggle-run evidence (screenshots, API responses) — large, regenerated each run
reports/toggle-run/

# Scenario-run evidence (screenshots, API responses) — large, regenerated each run
reports/scenario-run/

# Sync temp files
.!*
